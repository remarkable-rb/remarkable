en:
  remarkable:
    active_record:
      validate_presence_of:
        description: "require {{attributes}} to be set"
        expectation: "{{subject_name}} could not be saved if {{attribute}} is not set"
        missing:
          allow_nil?: "allow nil values for {{attribute}}"

